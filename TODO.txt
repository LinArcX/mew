---------------------------------------------------------------------------------
# v1
## general
- [+] open window in center of screen
- [+] when do alt+tab and when i hold this key combination, it should shows all open windows in a pop-up window.
  So i can cycle through them.(Alt+tab and Alt+Shift+tab for reverse cycle)
- [+] font should be high quiality everywhere.
- [+] right click for mew with these options:
  - [+] exit
- [P] keybinding file in: ~/.config/mew/keybindings
  - [+] define snap windows to left/right/bottom/top
  - [+] define maximize/minimize windows
- [+] config file in: ~/.config/mew/config
  - [+] background color
  - [+] login_sound
  - [+] logout_sound
  - [P] windows title fonts
    currently it's hard-coded. finally it should read from conf file.
  - [P] mouse theme
    currently it's hard-coded. finally it should read from conf file.
- [+] panel:
  - [+] show desktop (mandatory)
  - [+] time and date (mandatory)

## apps
### mpv
- [+] Left/right/bottom resize doesn't work

### nemo
- [+] bottom resize doesn't work.
- [+] left/right resize sometimes grab internal content, instead of grabbing the frame.
---------------------------------------------------------------------------------
# v2
## general
- [+] remove entriely context menu.
- [+] some windows are really small when you open them. put minimim size for all windows:
    width = 2/3 width of screen, height = 2/3 height of screen.

## ~/.config/mew/keybindings
- [+] define shortcut for fullscreen windows

## panel
- [+] time doesn't update.
- [+] when i click on desktop menu, it should show me desktop and minimze everything.
  if i click it again, it should bring back all windows into their previous positions.
- [+] logout currently doesn't logout when i click on it. when i click on time and date, it will logout.
- [+] put Desktop at right corner.
- [+] put logout at left corner.
- [+] currently when i maximize the windows, the panel goes on top ot them. and i can't see the bottom of my windows. i don't want this behavriour.
  panel should be reduce from whole screen size. every other window should calculate it's size and position based on remaining space in the screen(screen size should be minus panel size)

## ~/.config/mew/config
- [+] ability to define panel color.
- [+] for login_sound and logout_sound, remove dependency from mpg123 and play audio internally.
  if you want to use any library, it should be minimal and inform me before.
---------------------------------------------------------------------------------
# v3
- [+] create a start menu at left coerner. (Remove Logout and Keys from left of panel also)
  The start menu should be like in xfce or windows. but very minimal.
  what i want is that when i click on it, a pop up window should show. for the moment, it contains in vertical order.
   - Logout (use this icon: 󰗼 instead of wriging "Logout")
   - ShortKeys (use this icon:  instead of wriging "Keys")
- [+] i should be able to define a keybinding to bring back windows to center. center means:
        width = 2/3 width of screen, height = 2/3 height of screen.
- [+] use this icon:  instead of writing "Desktop" on panel.
- [+] put time after Desktop at right corner.
  for month, use the month name. for example instad of 09 you should write september.
---------------------------------------------------------------------------------
# v4
## general
- [+] when i do fullscreen, if i then press W-c to bring window to center, it won't work. (but snapping works, W-Left, W-Right, W-Up, W-Down)
- [+] right corner of panel should have this order(from right): first put "Desktop" icon, then put time/date
- [+] "Desktop" icon is on top of time/date. put some space between them. (bring time/data a little to the left to have more space between them)
- [+] in switcher, show the icons of applications. not just their text.
- [+] i can't see real icons in start menu and other places that should show the icon. fix it, if you can't at least tell me what should i do? 
  should i use another font?

## ~/.config/mew/config
- [+] background image
  choose a minimal approach. if you need extra libary to be use, just tell me know.

## panel
re-implement panel as a pluging system. (some plugins are mandatory. others are optional. you can enable/disable optional ones and recompile mew)
  - [P] volume icon (mandatory)
  - [P] add another entry in Start menu called: apps.
    when i click on it, a pop up window (in center of screen) should pops-up and it should shows all apps installed on my machine.
      (by searching .desktop files)
    it should have a search input text on top of itself. So i can type the name of the app.
    it should look like rofi.
  - [P] power manager (mandatory)
    - [P] reboot
    - [P] poweroff
    - [+] logout (exit from mew)
    - [P] mew --reconfigure
      not working as expected. need to test it again.
      - [+] put mew.pid in /tmp
---------------------------------------------------------------------------------
# v5
## panel
- [P] when i click on volume nothing happens. just when i click on  , it will works. why? the positioning is wrong?
  - [ ] it doesn't show the actual volume size, when i mute it, i can still hear the sound.
- [ ] when i hover over panel items, their color should change. also a hover text should be pop up and dissapear when i leave the pop-up
- [ ] define keybinding for each item in startmenu, like for apps, logout, etc..
- [ ] apps in start menu works. but it has different problems:
  - [+] it doen's have vertical scrool. so when i use down arrow key and go at the end of the list, it stops there and doesn't show more items. (i think you didn't implementing vertical scrolling throug items, right? do it) 
  - [+] when i start to type something, it can find the items. but it shows different frames(or windows) on top of each other.
  - [ ] it should be close, when i click outside of it.
  - [ ] power manager
    these items just log out. reboot, doesn't reboot actually. poweroff doesn't poweroff actually!
    - [ ] reboot
    - [ ] poweroff

## config
- [ ] ability to set panel items color + panel item hover color in config file.
- [ ] windows theme
- [ ] ability to define applications windows size or position. for example:
    mpv.pos.x = 0
    mpv.pos.y = 0
    mpv.width = 200
    mpv.height = 300

## panel
re-implement panel as a pluging system. (some plugins are mandatory. others are optional. you can enable/disable optional ones and recompile mew)
  - [ ] currently start menu closes only when i click on it. but i want it to be close  also whenever i clicked on other applications or another part of mew desktop.
  - [ ] network manager (mandatory)
  - [ ] language switcher (mandatory)
  - [P] power manager (mandatory)
    - [P] reboot
    - [P] poweroff
    - [P] mew --reconfigure
      not working as expected. need to test it again.

## apps
### mpv
- [ ] Pressing Enter doesn't maximize the whole window (i expect when i press ENTER in neovim, it becomes a full screen window)

### nemo
- [ ] when i extract zip files, there's a extract window appears, and its size is width: 2/3, height: 2/3. i don't want that.
    i want it to have the minimum size it can. and it shuould be centerized. also max button on it should be disabled.

### neovim
- [ ] imagine i open neovim, and also i open mpv on top. i close mpv with close buton, it shows me neovim.
  but cursor in neovim is empty rectangle instead of full rectangle.(maybe because the focus is not on neovim?)



---------------------------------------------------------------------------------
# v6
## panel
- [ ] apps popup in start-menu only close when i press ESCAPE from keyboard. i want it to be close, when i click on other apps, or in general space also in WM.
- [ ] instead of using amixer directly in Panel.cpp to mute audio, you should read the command from: ~/.config/mew/keybindings
  key="XF86AudioRaiseVolume", command="amixer set PCM 5%+" # pi5: amixer -D default set Master 5%+
  key="XF86AudioLowerVolume", command="amixer set PCM 5%-" # pi5: amixer -D default set Master 5%-
  key="XF86AudioMute", command="amixer -D pulse set Master toggle" # amixer -D pulse set Master toggle

  - [ ] Do the same process for RaiseVolue and LowerVolume.
    you need to update volume percentage number in panel when i increase/decrease the volume with keyboard shortcut. 
