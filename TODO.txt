# general
- [+] open window in center of screen
- [+] when do alt+tab and when i hold this key combination, it should shows all open windows in a pop-up window.
  So i can cycle through them.(Alt+tab and Alt+Shift+tab for reverse cycle)
- [+] font should be high quiality everywhere.
- [+] right click for mew with these options:
  - [+] exit
- [P] keybinding file in: ~/.config/mew/keybindings
  - [+] define snap windows to left/right/bottom/top
  - [+] define maximize/minimize windows
  - [ ] define key to bring back window to center.
- [ ] config file in: ~/.config/mew/config
  - [ ] size/position of windows.
  - [ ] background image
  - [ ] background color
  - [ ] login_sound
  - [ ] logout_sound
  - [ ] windows theme
  - [P] windows title fonts
    currently it's hard-coded. finally it should read from conf file.
  - [P] mouse theme
    currently it's hard-coded. finally it should read from conf file.
- [ ] implement panel as a pluging system. (some plugins are mandatory. others are optional. you can enable/disable optional ones and recompile mew)
  - [ ] show desktop (mandatory)
  - [ ] time and date (mandatory)
  - [ ] network manager (mandatory)
  - [ ] volume icon (mandatory)
  - [ ] language switcher (mandatory)
  - [ ] app launcher (mandatory)
    it shows .desktop files from ~/.local/share/apps
    you can also make simple apps using extension system of mew. they will appear hear automatically
    - [P] show keybindings (mandatory)
      currently, it's part of context menu. it should be a mandatory plugin of panel.
    - [ ] power manager (mandatory)
      reboot
      poweroff
      - [P] logout (exit from mew)
        currently, it's part of context menu. it should be a mandatory plugin of panel.
      - [ ] mew --restart
      - [P] mew --reconfigure
        - [ ] put mew.pid in /tmp

# apps
## mpv
- [ ] Enter doesn't maximize the window(it should also hide the all border & icons also)
- [ ] Left/right/bottom resize doesn't work

## nemo
- [ ] bottom resize doesn't work.
- [ ] left/right resize sometimes grab internal content, instead of grabbing the frame.

## neovim
- [ ] cursor size changed to rectanlgle

## tint2
- [ ] it should be on top of other windows.
- [ ] it should be part of mew whole screen and rest of screen size should be use for other apps. 
- [ ] it has border & icons. it shoulnd't have it.
- [ ] when i hover over it, the mouse cursor chagned to resize. it shouldn't.
